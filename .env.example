# Copy to .env and fill in. Never commit .env.
# Used by long-context baselines, LLM-as-judge eval, and answer generation.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=          # embeddings (text-embedding-3-large) + some baselines
GOOGLE_API_KEY=          # Gemini long-context baseline

# Hard ceiling enforced by eval/runner.py; refuses to start without it.
PERSONALMEM_RUN_BUDGET_USD=50
